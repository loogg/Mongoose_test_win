# Mongoose 源文件

# 收集当前目录的源文件
file(GLOB SRCS ${CMAKE_CURRENT_SOURCE_DIR}/*.c)

# 将源文件添加到父作用域变量
set(ALL_SOURCES ${ALL_SOURCES} ${SRCS} PARENT_SCOPE)

# 将头文件目录添加到父作用域变量
set(ALL_INCLUDE_DIRS ${ALL_INCLUDE_DIRS} ${CMAKE_CURRENT_SOURCE_DIR} PARENT_SCOPE)
